# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Greeter API
    description: The greeting service definition.
    version: 0.0.1
paths:
    /api/segment/get/{tag}:
        get:
            tags:
                - Greeter
            description: 号段模式
            operationId: Greeter_GenSegmentId
            parameters:
                - name: tag
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IDReply'
components:
    schemas:
        IDReply:
            type: object
            properties:
                id:
                    type: string
            description: 申请到的ID
tags:
    - name: Greeter
